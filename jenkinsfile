pipeline{
	agent any
	tools{nodejs 'Node'}
	environment{
		CI = 'true'
		}
		stages{
		stage('Install dependencies'){

		steps{
		 sh "npm install"
				}
				}
					stage('Build')
				{
				steps{
					sh "npm run build"
					}
				        }

		}
	}
